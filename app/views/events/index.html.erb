<div class="logo">
    <h1>
      na<div class="backward-letters">r</div>rua
    </h1>
  </div>
<div class="container">
  <div class="events">
    <% @events.each do |e| %>
      <div class="event">
        <div class="ename"><%= e.name %></div>
        <div class="einfo"><a href="#">+ info</a></div>
        <div class="eaddress"><%= e.address %></div>
      </div>
    <% end %>
  </div>
</div>